```
function calculaPrecoTotal(quantidade) {
  // Escreva seu código aqui
    let valorDaCompra = 0
  if(quantidade >= 12) {
    valorDaCompra = quantidade*1
    return valorDaCompra
  } else {
    valorDaCompra = quantidade*1.3
    return valorDaCompra
  }
}
```